apiVersion: 1

providers:
  - name: 'IAM Dashboards'
    orgId: 1
    folder: 'IAM 360'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
